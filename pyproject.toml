[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hermes-service-tui"
version = "0.1.0"
description = "Small dynamic TUI for managing Hermes Agent gateway and dashboard systemd user services on WSL2/Linux"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "textual>=0.80",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
hermes-service-tui = "hermes_service_tui.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/hermes_service_tui"]
